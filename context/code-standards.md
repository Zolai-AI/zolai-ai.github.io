# Code Standards — zolai-ai.github.io

- HTML5, valid semantic markup
- CSS custom properties for design tokens
- `prefers-reduced-motion` for accessibility
- Keep bundle tiny (no framework, no build)
